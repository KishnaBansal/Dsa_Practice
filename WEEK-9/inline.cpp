#include<iostream>
using namespace std;

int numShow(int n) {
    cout<<n<<endl;
}

int main() {
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);
    numShow(10);

}